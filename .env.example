# Predict API
PREDICT_URL=                        # Server-side predict API base URL
NEXT_PUBLIC_PREDICT_URL=/predict-api # Client-side predict API prefix (rewritten by Next.js)
NEXT_PUBLIC_PREDICT_WS_URL=wss://api.liberfi.io/prediction/api/v1/ws # Predict WebSocket URL (optional, enables real-time orderbook)
NEXT_PUBLIC_CENTRIFUGO_WS_URL=wss://api.liberfi.io/ws/v1 # Centrifugo client WS for World Cup live updates; staging uses wss://api.liberfi.io/staging/ws/v1

# Privy Authentication
NEXT_PUBLIC_PRIVY_APPID=            # Privy frontend App ID
PRIVY_APP_ID=                       # Privy server App ID
PRIVY_APP_SECRET=                   # Privy server Secret

# JWT
JWT_SECRET=                         # JWT signing secret
JWT_ISSUER=                         # JWT issuer claim
JWT_AUDIENCE=                       # JWT audience claim
JWT_EXPIRES_IN=24h                  # JWT expiration
JWT_COOKIE_NAME=predict-token       # JWT cookie name

# Cloudflare Turnstile
NEXT_PUBLIC_TURNSTILE_SITE_KEY=     # Public Turnstile sitekey for MiniApp captcha gate
TURNSTILE_SECRET_KEY=               # Server-side Turnstile secret key
MINIAPP_CAPTCHA_COOKIE_SECRET=      # Optional; falls back to JWT_SECRET
MINIAPP_CAPTCHA_COOKIE_NAME=miniapp_captcha_verified # MiniApp captcha verification cookie name
MINIAPP_CAPTCHA_COOKIE_MAX_AGE=86400 # MiniApp captcha cookie max age in seconds
MINIAPP_CAPTCHA_ALLOWED_HOSTNAMES=  # Optional comma-separated hostnames for Siteverify validation

# Telegram Mini App
NEXT_PUBLIC_TELEGRAM_MINI_APP_URL=https://t.me/liberfi_live_bot/liberfi_prediction_app # Telegram Mini App base URL for referral share links
TELEGRAM_BOT_TOKEN=                 # Server-side Telegram bot token for initData verification
TG_BOT_S2S_URL=                     # Server-side prediction-tg-bot base URL for multi-operator MiniApp verify
TG_BOT_S2S_API_TOKEN=               # Bearer token shared with prediction-tg-bot INTERNAL_API_TOKEN
TELEGRAM_BOT_USERNAME=              # Required stable bot username used in Telegram custom JWT subject, without @ preferred
TG_MINIAPP_COOKIE_NAME=tg_miniapp_context # Telegram Mini App context cookie name
TG_MINIAPP_COOKIE_MAX_AGE=86400     # Telegram Mini App context cookie max age in seconds
TG_MINIAPP_INITDATA_MAX_AGE=86400   # Telegram initData max accepted age in seconds
# TG_MINIAPP_COOKIE_SECRET=         # Optional dedicated signing secret; falls back to JWT_SECRET
TELEGRAM_PRIVY_JWT_ISSUER=          # Public issuer URL configured in Privy JWT-based auth
TELEGRAM_PRIVY_JWT_AUDIENCE=privy   # Audience configured in Privy JWT-based auth
TELEGRAM_PRIVY_JWT_PRIVATE_KEY=     # Server-only RS256/ES256 private key for Telegram Privy JWT-based auth
TELEGRAM_PRIVY_JWT_KEY_ID=          # kid exposed by /api/well-known/jwks
# TELEGRAM_PRIVY_JWT_ALG=RS256      # Optional; defaults to RS256

# MPChat Mini App
NEXT_PUBLIC_ENABLE_MPCHAT_MINIAPP=false # Enable MPChat MiniApp SDK + automatic Privy JWT auth
NEXT_PUBLIC_MPCHAT_MINI_APP_URL=https://mp.net/liberfi_live_bot/liberfi_prediction_app # MPChat Mini App base URL for referral share links
MPCHAT_BOT_TOKEN=                  # Server-side MPChat bot token for initData verification
MPCHAT_BOT_USERNAME=               # MPChat bot username, without @ preferred
MPCHAT_INITDATA_MAX_AGE=300        # MPChat initData max accepted age in seconds
MPCHAT_MINIAPP_COOKIE_NAME=mp_miniapp_context # MPChat MiniApp context cookie name
MPCHAT_MINIAPP_COOKIE_MAX_AGE=604800 # MPChat MiniApp session cookie max age in seconds
# MPCHAT_MINIAPP_COOKIE_SECRET=    # Optional dedicated signing secret; falls back to JWT_SECRET
MPCHAT_PRIVY_JWT_ISSUER=           # Public issuer URL configured in Privy JWT-based auth
MPCHAT_PRIVY_JWT_AUDIENCE=privy    # Audience configured in Privy JWT-based auth
MPCHAT_PRIVY_JWT_PRIVATE_KEY=      # Server-only RS256/ES256 private key for Privy JWT-based auth
MPCHAT_PRIVY_JWT_KEY_ID=           # kid exposed by /api/well-known/jwks
# MPCHAT_PRIVY_JWT_ALG=RS256       # Optional; defaults to RS256
NEXT_PUBLIC_PRIVY_SESSION_SIGNER_ID= # Session signer / key quorum id configured in Privy
NEXT_PUBLIC_PRIVY_SESSION_SIGNER_POLICY_IDS= # Optional comma-separated session signer policy ids

# Feature flags
# Enable the Kalshi venue. Unset / empty / false → Kalshi is disabled: the
# Kalshi wallet block and aggregate balance summary are hidden in the header,
# and the markets list is pinned to Polymarket only (platform filter hidden).
# NEXT_PUBLIC_ENABLE_KALSHI=true

# Hide the Sports and Esports entries from header/footer navigation while
# keeping their routes available for direct access. All environments default
# to visible.
# NEXT_PUBLIC_ENABLE_SPORTS_NAVIGATION=false

# Analytics (optional)
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-482RQNZD1J # Google Analytics Measurement ID

# Local SDK link (development only)
# Point @liberfi.io/* imports + CSS at the sibling react-sdk source tree for
# live HMR instead of the published npm dist. Both must be set; takes effect
# only when NODE_ENV !== production. See AGENTS.md "本地 App 与 SDK 联调步骤".
# USE_LOCAL_SDK=true                # Opt-in switch for local SDK link mode
# LOCAL_SDK_ROOT=../react-sdk       # Path to react-sdk root, relative to repo root
